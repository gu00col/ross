/**
 * JavaScript para a página principal do dashboard
 * Sistema ROSS - Analista Jurídico
 * Versão mínima - dados são renderizados pelo PHP
 */

// Inicializar quando o DOM estiver carregado
document.addEventListener('DOMContentLoaded', function() {
    // Verificar se estamos na página de dashboard
    if (document.querySelector('.dashboard-page')) {
        initDashboardIndex();
    }
});

/**
 * Inicializar funcionalidades da página principal do dashboard
 */
function initDashboardIndex() {
    // Apenas funcionalidades essenciais que não interferem com modais
    // Dados são renderizados pelo PHP, não precisamos de JavaScript complexo
}
